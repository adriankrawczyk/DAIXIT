import { Cloud } from "@react-three/drei";
import React from "react";

const GalaxyShape = ({ position, rotation, opacity, scale, color }) => {
  return (
    <></>
    // <Cloud
    //   position={position}
    //   rotation={rotation}
    //   scale={scale}
    //   opacity={opacity}
    //   speed={0.2}
    //   depth={1.5}
    //   segments={20}
    //   color={color}
    // />
  );
};

export default GalaxyShape;
